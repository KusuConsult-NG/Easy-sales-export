import { createRequire } from 'module';
import { test, expect } from '@jest/globals';

// For simplicity, we can just run this as a small custom jest script 
// rather than rewriting the module system.
