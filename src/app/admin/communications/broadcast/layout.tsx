export const maxDuration = 60; // Extend Vercel Hobby execution limit from 15s to 60s

export default function BroadcastLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
